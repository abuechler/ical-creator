{
  "name": "workspace",
  "version": "1.2.1",
  "description": "A simple browser-based tool for creating iCal (.ics) calendar event files.",
  "main": "index.js",
  "directories": {
    "doc": "doc"
  },
  "scripts": {
    "test": "playwright test tests/ical-creator.spec.js",
    "test:all": "playwright test",
    "test:headed": "playwright test --headed",
    "test:online": "playwright test tests/online-validator.spec.js",
    "test:report": "playwright show-report",
    "validate": "cd validator && ./mvnw package -q && cd .. && for f in test-output/*.ics; do java -cp validator/target/ical-validator-1.0.0.jar:validator/target/lib/* ICalValidator \"$f\"; done"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
